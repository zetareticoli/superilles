{% extends "layout/base.njk" %}
{% from "macro/story.njk" import story %}

{% block content %}

<section class="g-section g-headlines g-headlines--preview">
  {{ story("lead", "cover", "lg", "datestamp", "summary", "sublinks") }}

  {{ story("featured", "stack", "md", "datestamp", "summary") }}

  {{ story("featured", "stack", "md", "datestamp", "summary") }}
</section>

<section class="g-section g-list">

  {% for i in range(0,5) %}
    {{ story("featured", "list", "md", "datestamp", "summary", "sublinks") }}
  {% endfor %}

</section>

{% endblock %}